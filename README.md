# Linea Learning Platform Monorepo

This repository has been restructured into a monorepo containing a Laravel backend and a React frontend.

- `backend/` – Laravel 11 API skeleton
- `frontend/` – React 19 TypeScript SPA
- `infra/` – Docker and deployment configuration
- `docs/` – documentation and architecture diagrams

This is a placeholder README; further documentation is located in `docs/`.
